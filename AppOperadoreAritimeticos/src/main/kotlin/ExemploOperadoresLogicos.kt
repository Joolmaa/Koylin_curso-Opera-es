fun main(){

    var valorA:String = "Brasília"
    var valorB:String = "Campo Grande "
    var valorC:String = "Brasília"

    // and ou $$ e Lógico
    // or ou o ou || ou lógico
    // not lógico !=

    //var comparacao = (valorA.equals(valorB) && valorA.equals(valorC))
    //var comparacao = (valorA.equals(valorB) || valorA.equals(valorC))

    var comparacao = (valorA.equals(valorB) != valorA.equals(valorC))
    print(comparacao)



}